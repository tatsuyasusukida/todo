extends ../layout

block title
  title Todoの登録完了｜ToDoリスト管理アプリ

block breadcrumb
  nav.mt-3(aria-label='パンくずリスト')
    .container-fluid
      ol.breadcrumb
        li.breadcrumb-item
          a(href='../../') ToDo
        li.breadcrumb-item
          a(href='../') 登録
        li.breadcrumb-item
          a(href='./' aria-current='page') 完了

block main
  main
    .container-fluid
      h1 ToDoの登録登録
      nav.mt-3(aria-label='メイン')
        .d-grid
          a.btn.btn-success(href='../../1/') ToDoを表示する
